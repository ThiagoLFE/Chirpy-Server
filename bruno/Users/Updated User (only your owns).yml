info:
  name: Updated User (only your owns)
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "email": "sweetplum@gmail.com",
        "password": "bananaSplit"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
