info:
  name: Updated User (just yourself)
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:8080/api/users
  body:
    type: json
    data: |-
      {
        "email": "sweetplum@gmail.com",
        "password": "bananaSplit2"
      }
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
