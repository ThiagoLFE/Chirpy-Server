info:
  name: Create Chirp
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/chirps
  body:
    type: json
    data: |-
      {
        "body": "teste"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
