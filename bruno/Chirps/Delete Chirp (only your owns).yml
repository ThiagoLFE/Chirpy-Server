info:
  name: Delete Chirp (only your owns)
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:8080/api/chirps/c4bcd112-16bb-4109-8c4c-3c4c84ddf488
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
