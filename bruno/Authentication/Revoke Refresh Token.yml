info:
  name: Revoke Refresh Token
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/api/revoke
  auth:
    type: bearer
    token: "{{refreshToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
