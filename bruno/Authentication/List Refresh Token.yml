info:
  name: List Refresh Token
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8080/api/refresh_tokens
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
