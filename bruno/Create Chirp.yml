info:
  name: Create Chirp
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/api/chirps
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
