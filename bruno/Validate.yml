info:
  name: Validate
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8080/api/validate_chirp
  body:
    type: json
    data: |-
      {
        "body": "teste"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
